# AI-Powered Field Workforce Job Marketplace

Phase 1: Supabase email OTP authentication + role-based dashboard routing.

## Frontend
cd frontend
npm install
npm run dev

## Backend
cd backend
python -m venv .venv
.venv\\Scripts\\activate
pip install -r requirements.txt
uvicorn app.main:app --reload
